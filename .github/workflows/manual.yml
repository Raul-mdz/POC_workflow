name: 🧹 JIRA Branch Cleanup

on:
  schedule:
    # Ejecutar todos los días a las 2:00 AM UTC (puedes ajustar el horario)
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      days_old:
        description: 'Días de antigüedad para considerar obsoleta'
        required: false
        default: '10'
        type: string

jobs:
  cleanup-jira-branches:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: read
      actions: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Git
        run: |
          git config --global user.email "action@github.com"
          git config --global user.name "GitHub Action"

      - name: Authenticate GitHub CLI
        run: echo "${{ secrets.GITHUB_TOKEN }}" | gh auth login --with-token
        
      - name: Verify GitHub CLI authentication
        run: gh auth status

      - name: Fetch all branches
        run: git fetch --all --prune

      - name: 🧹 Cleanup obsolete JIRA branches
        env:
          GH_REPO: ${{ github.repository }}
          DAYS_OLD: ${{ inputs.days_old }}
        run: |
          set -e
          LIMIT_DATE=$(date -u -d "$DAYS_OLD days ago" +%s)
          echo "🧹 Eliminando ramas JIRA obsoletas (sin actividad en últimos $DAYS_OLD días)"
          echo "   Límite: $(date -u -d "$DAYS_OLD days ago" +%Y-%m-%d)"
          echo "   Timestamp límite: $LIMIT_DATE"
          echo "   Criterio: Eliminar ramas con último commit anterior a esta fecha"
          echo ""

          PROTECTED_REGEX='^(main|dev|feature/Vuexy-4\.0\.0-starter-kit-init-template|feature/Vuexy-4\.0\.0-full-version-init-template|docs/update-readme-and-changelog)$'

          echo "📦 Descargando lista de PRs (open/closed/merged)..."
          gh pr list --state all --json headRefName,state,number > prs.json

          FOUND=0
          DELETED=0
          KEPT=0

          echo "📋 REVISANDO RAMAS JIRA:"
          echo "========================"
          
          # Crear array de ramas para procesar
          BRANCHES=()
          while IFS= read -r branch; do
            BRANCHES+=("$branch")
          done < <(git for-each-ref --format='%(refname:short)' refs/remotes/origin/ | \
            grep -E '^origin/[A-Z]+-[0-9]+' | \
            grep -v -E "^origin/($PROTECTED_REGEX)$")
          
          echo "🔍 Encontradas ${#BRANCHES[@]} ramas JIRA para analizar"
          
          for branch in "${BRANCHES[@]}"; do
            BRANCH_NAME="${branch#origin/}"
            echo "::group::🧩 $BRANCH_NAME"

            LAST_COMMIT_DATE=$(git log -1 --format="%cI" "$branch" || echo "unknown")

            if [ "$LAST_COMMIT_DATE" = "unknown" ]; then
              echo "⚠️ No se pudo obtener la fecha de último commit para $BRANCH_NAME"
              echo "::endgroup::"
              continue
            fi

            LAST_COMMIT_EPOCH=$(date -d "$LAST_COMMIT_DATE" +%s)
            PR_STATE=$(jq -r --arg b "$BRANCH_NAME" '.[] | select(.headRefName==$b) | .state' prs.json)
            PR_NUMBER=$(jq -r --arg b "$BRANCH_NAME" '.[] | select(.headRefName==$b) | .number' prs.json)
            
            echo "📊 Análisis de $BRANCH_NAME:"
            echo "   Último commit: $LAST_COMMIT_DATE"
            echo "   Timestamp: $LAST_COMMIT_EPOCH"
            echo "   Límite: $LIMIT_DATE"
            echo "   Días sin actividad: $(( (LIMIT_DATE - LAST_COMMIT_EPOCH) / 86400 ))"
            echo "   PR State: $PR_STATE"
            echo "   PR Number: $PR_NUMBER"

            DELETE_BRANCH=false

            if [ "$PR_STATE" = "OPEN" ]; then
              echo "🚫 Conservando (PR abierto #$PR_NUMBER)"
              KEPT=$((KEPT + 1))
            elif [ "$PR_STATE" = "MERGED" ]; then
              echo "🟢 Eliminando (PR #$PR_NUMBER mergeado)"
              DELETE_BRANCH=true
            elif [ "$PR_STATE" = "CLOSED" ]; then
              echo "🟠 Eliminando (PR #$PR_NUMBER cerrado sin merge)"
              DELETE_BRANCH=true
            elif [ "$LAST_COMMIT_EPOCH" -lt "$LIMIT_DATE" ]; then
              echo "🗑️ Eliminando (último commit hace más de $DAYS_OLD días: $LAST_COMMIT_DATE)"
              DELETE_BRANCH=true
            else
              echo "✅ Conservando (último commit reciente: $LAST_COMMIT_DATE)"
              KEPT=$((KEPT + 1))
            fi

            if [ "$DELETE_BRANCH" = true ]; then
              echo "🧹 Eliminando rama remota origin/$BRANCH_NAME..."
              if git push origin --delete "$BRANCH_NAME" 2>&1; then
                echo "✅ Rama $BRANCH_NAME eliminada exitosamente"
                DELETED=$((DELETED + 1))
              else
                echo "⚠️ Error al eliminar $BRANCH_NAME - intentando con GitHub CLI..."
                if gh api -X DELETE "repos/$GH_REPO/git/refs/heads/$BRANCH_NAME" 2>&1; then
                  echo "✅ Rama $BRANCH_NAME eliminada con GitHub CLI"
                  DELETED=$((DELETED + 1))
                else
                  echo "❌ No se pudo eliminar $BRANCH_NAME con ningún método"
                fi
              fi
            fi

            FOUND=1
            echo "::endgroup::"
          done
          
          echo "🔍 Procesamiento completado. FOUND=$FOUND"

          echo ""
          echo "📊 RESUMEN FINAL:"
          echo "=================="
          echo "🟢 Ramas eliminadas: $DELETED"
          echo "✅ Ramas conservadas: $KEPT"
          echo "📈 Total analizadas: $((DELETED + KEPT))"

          if [ "$FOUND" -eq 0 ]; then
            echo "✅ No se encontraron ramas JIRA para analizar."
          fi
          
          # Verificar que las ramas se eliminaron correctamente
          if [ "$DELETED" -gt 0 ]; then
            echo ""
            echo "🔄 Verificando eliminación de ramas..."
            git fetch --all --prune
            echo "✅ Verificación completada"
          fi
